{
  "name": "word-mining",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "front:dev": "npm run dev --prefix front",
    "server:dev": "npm run dev --prefix server",
    "setup": "npm install --prefix server && npm install --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
